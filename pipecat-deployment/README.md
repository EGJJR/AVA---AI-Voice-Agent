# Pipecat deployment

Placeholder for deployment config and scripts (e.g. Daily.co / Twilio SIP dial-in, Lambda, or other runtimes).

The `daily-twilio-sip-dial-in` subfolder is used for Twilio SIP integration with Daily.co. Create a virtualenv and add any deployment-specific code or config here.
